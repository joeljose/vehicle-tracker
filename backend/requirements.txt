# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Utilities
numpy>=1.24.0
opencv-python-headless>=4.8.0

# Note: pyds (DeepStream Python bindings) is pre-installed in the
# DeepStream container image. Do not add it here.
# GStreamer Python bindings (gi) are also pre-installed.
